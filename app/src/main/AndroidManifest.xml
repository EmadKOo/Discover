<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.discover">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.UserCountryActivity.UserCountryActivity" android:theme="@style/AppThemend"></activity>
        <activity
            android:name=".ui.DisplayNewsActivity.DisplayNewsActivity"
            android:theme="@style/AppThemend" />
        <activity
            android:name=".ui.SplashActivity.SplashActivity"
            android:theme="@style/AppThemend">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.SourceActivity.SourceActivity"
            android:theme="@style/AppThemend" />
        <activity
            android:name=".ui.MainActivity.MainActivity"
            android:theme="@style/AppThemend"
            android:windowSoftInputMode="stateHidden" />
    </application>

</manifest>